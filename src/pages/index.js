import Dashboard from 'components/hubDashboard';

const Hub = () => (
  <div style={{ display: 'flex', flexDirection: 'column', flex: 1 }}>
    <Dashboard />
  </div>
);

export default Hub;
